# Eagle Abstract — Homepage Mock

Redesign concept for eagleabstract.com. FastAPI static server, Railway-ready.

- `static/index.html` — the homepage mock
- `SCOPE.md` — full three-tier project scope
- Healthcheck: `/healthz`
- Run locally: `pip install -r requirements.txt && uvicorn app.main:app --reload`
